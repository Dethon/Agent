:443 {
    tls internal {
        on_demand
    }

    # SignalR hub - proxy to agent
    handle /hubs/* {
        reverse_proxy agent:8080
    }

    # Everything else - proxy to webui
    handle {
        reverse_proxy webui:8080
    }
}
